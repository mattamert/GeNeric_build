config("default_include_dirs") {
  include_dirs = [
    "//",
    root_gen_dir,
  ]
}

config("default_compiler_options") {
  #TODO: This is the Windows syntax. The Linux/Mac syntax would be "-std=c++17"
  cflags_cc = [ "/std:c++17" ]
}